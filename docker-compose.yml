services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: railng
      POSTGRES_PASSWORD: railng
      POSTGRES_DB: railng
    ports:
      - "5432:5432"
    volumes:
      - railng_pg:/var/lib/postgresql/data

volumes:
  railng_pg:
